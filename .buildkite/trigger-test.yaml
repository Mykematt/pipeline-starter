steps:
  - label: ":rocket: Pipeline A - Main tests"
    command: |
      echo "Running Pipeline A tests"
      echo "Commit: ${BUILDKITE_COMMIT}"
      sleep 3
  
  - trigger: "buildkite-tests"
    label: ":arrow_right: Trigger Pipeline B"
    async: true
    build:
      message: "${BUILDKITE_MESSAGE}"
